# rex

bittrex cli tool.

### prerequisites

- account at bittrex.com
- set up golang: https://golang.org

### get up and running

`go install`

ensure you have a`.env` file, configured like so:

```bash
KEY=
SECRET=
```